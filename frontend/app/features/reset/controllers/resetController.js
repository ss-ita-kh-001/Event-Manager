(function() {

    angular.module('em.reset').controller('em.reset.resetController', resetController);

    function resetController($scope, $location, authenticationService, flashService, userService, $auth, $rootScope) {
      console.log('resetCTRL');

    }
})();

describe('register page', function() {
    var ptor;
    beforeEach(function() {
        browser.get('http://127.0.0.1:9000/');
        ptor = protractor.getInstance();
    });

    it('should load the  page', function() {
        var ele = by.model('selectedGame');
        expect
    });
});